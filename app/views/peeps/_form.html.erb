<%= form_with(model: peep, local: true) do |form| %>
  <% if peep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peep.errors.count, "error") %> prohibited this peep from being saved:</h2>

      <ul>
      <% peep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :peep %>
    <%= form.text_field :peep %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
